@model IEnumerable<New_Stock_Management.Models.Bill_Payment_Master>

@{
    ViewData["Title"] = "Index";
}

<h1>Bill Payment Master</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Company_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Amount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Payment_Date)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Company_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Amount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Payment_Date)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Bill_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Bill_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Bill_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
